# S38 Upload-to-Packet Discovery — run point

## 2026-07-14 — checkpoint 1

- Added a server-backed IDI report intake contract. The browser uploads only to the existing verified artifact store; the server reads that artifact back before parsing it.
- Added bounded PDF, DOCX, and CSV extraction. Image, scanned-PDF, and legacy-DOC input route through the Google Workspace conversion path and fail closed until Workspace is connected.
- Persisted IDI imports per estate with artifact hash, extraction provenance, source locators, deterministic contact confidence, and idempotent duplicate handling. Raw extracted report text is not returned to the browser or persisted in the Discovery record.
- Added the subtle Discovery CTA control path that automatically starts the existing one-estate Discovery runner only after verified extraction succeeds.
- Local proof: `pnpm --filter @ple/artifact test` passed after the new worker and UI contract checks.
- Remaining before release: full Google Workspace connection/destination UI, immutable-PDF-to-Drive delivery handoff, browser-visible end-to-end proof, production deploy, and final audit.

## 2026-07-14 — checkpoint 2

- Added a temporary production-only trusted-IP bypass. It requires an exact IP, an explicit enable flag, a future expiry, and the Vercel runtime; normal Google-session enforcement remains unchanged for every other request. The browser gate and protected API handlers share the same rule.
- Added Google Workspace OAuth expansion, encrypted server-to-Worker connection storage, per-user Drive-folder selection, and immutable PDF delivery with Drive file-ID, parent-folder, size, and internal-delivery readback. Discovery now refuses to mark its final phase complete until that delivery succeeds.
- Browser proof on the actual local surface: the visible IDI-report button opened the native chooser; a CSV upload passed attachment readback and extraction; its source-located high-confidence candidate persisted; then the app invoked the automatic external source run. A rerender-time file-input reset error was found and fixed before the follow-up run.
- Browserbase preflight: the supplied API key can list projects, but a single disposable 60-second session returned `402 Payment Required`. No session was created. This is an external Browserbase billing/capacity blocker for the Tax Collector browser workflow; the app keeps Discovery blocked instead of fabricating receipt evidence.
- Remaining before release: full suite/review, production deploy and trusted-IP activation, live deployed interaction proof, and the client-call Google Workspace consent plus folder selection/readback.

## 2026-07-14 — checkpoint 3

- Rebuilt Document Prep as one connected operational shell: a clear primary Discovery control, a right-aligned `Upload IDI Report` action, modern document rows that open from the whole card, and a compact dossier preview rail that preserves a readable working area at desktop size.
- Kept the implementation dependency-free. The app already has maintained OSS headless/floating primitives where they fit; adding a general-purpose component kit to this plain HTML surface would create a second visual system without improving the actual workflow controls.
- Added an explicit ChatGPT Work handoff that delivers the verified immutable packet to the chosen Google Drive folder, copies a review-only brief, then opens ChatGPT. The handoff excludes raw IDI content and credentials, requires the packet readback first, and asks the operator to choose Work before pasting the brief.
- Browser regression coverage now exercises card-click preview, the desktop rail/document geometry, the exact upload label and position, manual IDI artifact readback/extraction/run, and the safe ChatGPT Work brief boundary.

## 2026-07-14 — checkpoint 4: architecture and design baseline

- Re-read the current shared Solvys design canon, the HeirRight workflow checklist and source packets, the Fintheon shell/rail implementation, and the active S38 orchestration briefs before touching product source.
- Locked the build target as the HeirRight Case Journey Cockpit: Fintheon supplies only shared-chrome, rounded-main, and push-rail geometry; HeirRight retains its graphite/cream identity, real-estate vocabulary, stop rules, selected-estate contract, and human-review gates.
- Confirmed the active branch remains `feat/idi-report-intake-demo` in the isolated `/private/tmp/heirright-idi-report-intake-demo` worktree, with the substantial pre-existing S38 changes preserved and the canonical checkout untouched.
- Fresh baseline proof passed before the architecture extraction: `pnpm turbo run build --force`, the complete `@ple/artifact` test suite, and `git diff --check`. The suite includes 175 button contracts plus the IDI upload, Google Workspace, route-auth, packet, workspace-state, and product-loop regressions.
- Environment proof: Node 25.8.1, pnpm 9.15.0, Vercel CLI 53.2.0, and `expect-cli` 0.0.15 are available. Wrangler is intentionally invoked through the worker's pinned pnpm dependency because no global binary is installed.
- The earlier dependency-free UI decision is superseded by TP's explicit approval of `@awesome.me/webawesome` Free, `ag-grid-community`, and esbuild. All runtime assets remain self-hosted and no Pro or Enterprise package is authorized.

## 2026-07-14 — checkpoint 5: current production truth

- Re-verified all release identities before implementation: GitHub authentication is valid, Cloudflare Wrangler is authenticated to the configured account, and the production UI project is `solvys/heirright-landing-demo` rather than the similarly named marketing-site project.
- Vercel currently maps `heirright-landing-demo.vercel.app` and `surface.heirright.com` to production deployment `dpl_2HjpQNyg1k8LK4fRkuptb8rDvuyh`. The stable Vercel alias renders the existing HeirRight app in the Codex in-app browser with no console warnings or errors.
- `surface.heirright.com` still fails public DNS resolution from the Codex browser even though Vercel has assigned the alias. The release proof must therefore distinguish an assigned Vercel alias from a publicly resolvable customer hostname.
- The current production UI still opens on Document Prep behind an uncleared Google sign-in gate, and Google OAuth is visibly unconfigured. The exact-IP, explicit-expiry bypass is not active on the current deployment, so the new release must configure and prove that temporary access path before it can claim live end-to-end interaction.

## 2026-07-14 — checkpoint 6: T1 runtime foundation approved

- Extracted the monolithic artifact into an esbuild entry, an auth-gated legacy compatibility layer, semantic graphite/cream tokens, a feature registry, a persistent single-rail registry, a theme store, and a Nucleo icon facade. Feature views, commands, rail actions, and lifecycle work remain disabled until the authorized bridge is installed.
- Pinned Web Awesome Free `3.10.0`, AG Grid Community `36.0.0`, and esbuild `0.28.1`; removed the unused direct Headless UI and Floating UI dependencies. The build self-hosts runtime assets and rejects runtime CDNs, Web Awesome imports outside the approved Free allowlist, and AG Grid Enterprise across manifests, source, output, and the lockfile.
- Added an executable runtime contract covering deterministic clean builds, zero-config feature discovery, bridge redaction, selected-estate safety, fail-closed startup and rollback, teardown isolation, rail persistence and mobile transience, subscriber failure isolation, and dark/cream text, focus, and control-boundary contrast.
- Dedicated T1 review approved the architecture with no code blocker. The complete artifact suite passes with 176 button contracts plus IDI upload, Google Workspace, route-auth, packet, workspace-state, and product-loop regressions; `git diff --check` is clean.
- Codex in-app browser parity remains an explicit overall release gate. Its platform availability check currently returns no browser surfaces, so no Chrome or standalone-browser substitute was used and no rendered T1 claim was made from weaker evidence.

## 2026-07-14 — checkpoint 7: local packet completion decoupled from Google

- Discovery now completes, persists, links, and exposes its verified HeirRight PDF before the optional Google follow-on begins. A missing Google connection becomes guided setup, and a failed Drive delivery leaves the local packet complete with a review notice instead of rolling the automation timeline back to blocked.
- ChatGPT Work can now open from the verified local packet, copy the sanitized review brief, and expose the packet link before Google is configured. When Drive is available, delivery and readback remain the preferred enhancement; credentials and raw IDI text remain outside the handoff.
- The operator-facing file chooser now advertises only PDF and DOCX, while bounded CSV parsing remains a backend compatibility path for existing regression coverage.
- Focused proof passed: clean artifact build, updated product-loop contract, and `git diff --check`.

## 2026-07-14 — checkpoint 8: Case Journey and operational surfaces integrated

- Added the Dashboard-first Case Journey Cockpit, persistent collapsible icon rail, contextual header and footer, user-selectable dark/cream/system themes, a single resizable push rail, and a full-height focus-contained mobile sheet. The lifecycle uses the operator vocabulary Move Forward, Review, Blocked, and Move On without importing Fintheon's color or trading semantics.
- Replaced Document Prep with an estate-pinned PDF/DOCX intake, inline recovery, truthful upload-to-packet automation timeline, editorial whole-row document controls, and rail-owned document/completion actions. Local packet completion remains independent of Google; ChatGPT Work and the verified Google handoff remain explicit completion actions.
- Added AG Grid Community views only for Estates, Queue, and the Admin activity dataset. The integration review corrected queue selection so the grid's exact estate IDs reach the queue, and corrected Google delivery so the button sends the already verified local packet through Drive readback rather than generating a different export.
- Hardened visible document actions: generated packet records cannot expose a no-op removal control, the IDI source report cannot use the generic supporting-file replacement path, and unsupported removals now fail visibly instead of resolving as success.
- First integrated esbuild bundle passed with deterministic discovery of exactly three feature registrations. All new feature JavaScript passed syntax checks and `git diff --check` is clean. Focused T2/T3 contracts and Codex-browser interaction proof remain the next gates.

## 2026-07-14 — checkpoint 9: real bridge and review repairs

- The complete artifact suite passed after shell and Document Prep integration, including Worker compilation, route/auth/storage/packet regressions, IDI and Google Workspace contracts, the product-loop test, and 216 executable button contracts.
- A dedicated review then found three real bridge omissions hidden by fixture-only UI tests: server-approved high-confidence IDI contacts did not clear contact review, verified Official Records and obituary facts did not hydrate the Discovery phases, and the public snapshot did not expose the verified uploaded IDI row. Each path now has a focused production-source contract, and the IDI snapshot row is sanitized so it exposes no contacts, extracted text, content hash, or storage URL.
- ChatGPT Work refresh now returns through the registered Document Prep renderer, packet revisions remain monotonic beyond the capped audit history, and the public Estates dataset exposes only sanitized lead/missing-info categories needed by approved Community-grid filters.
- The standalone Playwright runner could not launch Chromium on this host because macOS rejected its Mach-port registration before any page opened. The Codex in-app browser connection also became unavailable during the follow-up attempt, so neither condition is being represented as UI proof; the required local and production Codex-browser passes remain open.
- `pnpm audit --prod` could not reach the npm audit endpoint because local DNS returned `ENOTFOUND`. This is an unresolved environment gate to retry before release, not a clean audit result.

## 2026-07-14 — checkpoint 10: worktree recovery and focused proof

- The disposable `/private/tmp` worktree was removed by a host restart while its branch reference and canonical checkout remained intact. Recreated the branch worktree at the same path, replayed the recorded successful patch stream in timestamp order, and reran the one recorded mechanical source split and pinned dependency installs. No canonical tracked file was changed during recovery.
- A clean workspace bootstrap exposed that the Worker depends on the generated `@ple/types` output; building the types package first restored a clean Worker typecheck. The final test gate must exercise the root build graph so this ordering is never supplied by stale local artifacts.
- Focused recovery proof is green for the esbuild artifact, Worker typecheck, component runtime, shell, Document Prep, IDI intake, Google Workspace, and product-loop contracts.
- The recovery run also exposed two genuine T3 mismatches: the rail contract expected a visible document preview but rendered only a generic overview, and the new-report reset contract called a helper that had never been exported. The rail now renders a labeled preview surface and file selection resets prior timeline/revision state through one tested helper.
- Release remains blocked on the active security lanes, full local suite, Codex in-app browser interaction proof, dedicated final review, commit, and exact-commit production deployment.

## 2026-07-14 — checkpoint 11: hostile review and workflow continuity

- Closed the ordinary-operator paid-run smuggling path by replacing the upload extractor's browser-body spread with a strict uploaded-import allowlist. Paid requests now stay behind the exact-admin gate, and stale or ambiguous vendor reservations require a descriptive administrator override instead of silently risking a second charge.
- Repaired the preflight product findings: company-owned and recent-sale stop rules now reach the Case Journey disposition, Queue nested controls keep their own Enter/Space behavior, remaining legacy glass rims are neutralized, and Document Prep exposes working Discovery and Closing Prep workflows rather than relabeling Closing Prep as Discovery.
- Restored refresh continuity without restoring browser PII persistence. The authenticated app can reload the canonical server-stored IDI import and its reviewed candidates into memory for the selected estate, while raw report text, contact review state, credentials, hashes, and private file URLs remain outside local/session storage.
- Hardened access administration so exact emails never expand into public-domain access. Local auth-off development changes can mutate a verified local config; production changes now return a truthful approval-request state and leave the deployed sign-in allowlist unchanged until the environment update is approved and deployed.
- Added bounded PDF page/text traversal, DOCX central-directory entry/uncompressed-size/compression-ratio checks, aggregate locator limits, and linear-time bounded contact deduplication. Real PDF/DOCX handler fixtures, canonical Discovery provenance, static-build privacy, and the full suite remain active gates before browser proof.

## 2026-07-14 — checkpoint 12: replacement, privacy, and production-auth closure

- Made verified IDI replacement a real PDF/DOCX workflow. The Document Prep control and document rail now let an exact configured Google administrator choose the corrected report, require a descriptive reason, stop an old in-flight automation, and start a fresh Discovery run whose packet becomes the active output.
- Closed client-side orphan retention for both generic supporting files and IDI intake. A failed post-upload readback/import now requests deletion and accepts only verified deletion or a verified not-found response; an unverified cleanup becomes an explicit administrator storage-review blocker.
- Unified production auth semantics with the nested route guards. Root-issued Google sessions now carry `mode: google`, the session response exposes a server-computed exact-admin capability, and the temporary exact-IP bypass remains non-admin. Production route tests cover the admin replacement gate and trusted-IP rejection.
- Locked browser-history restores before they can reveal cached estate DOM, added no-store HTML and framing/content security headers locally and in both Vercel configurations, and routed the production canonical IDI GET wrapper directly to its GET/POST handler.
- Removed realistic-looking sample names, deliverable addresses, and folios from the unauthenticated bundle. Public demo rows are now labeled synthetic samples with invalid `00000` addresses and reserved fixture folios, while the build rejects both the former production seed and former demo values.
- Focused proof is green for the artifact build, static-build privacy, shell/security contracts, Document Prep replacement behavior, root route-auth parity, and canonical IDI reload. The active Worker provenance/contact/paid-result lane, Google temporary-file rollback hardening, full suite, Codex-browser proof, final review, commit, and deployment remain release gates.

## 2026-07-14 — checkpoint 13: canonical source-save boundary and dependency preflight

- Removed the optimistic manual-source path. Save Capture, Run Source Search, and the automatic Discovery source step now wait for verified canonical Discovery File readback before applying evidence; failed or ambiguous writes restore the prior capture and add a blocker without retaining the unsaved form payload in readiness state.
- Made both artifact fallbacks fail closed with `source_capture_store_unavailable` when the Worker-backed store cannot be reached. Verified source-only Discovery records can hydrate before a dossier exists, so a durable manual capture survives reload without being mistaken for a completed source run.
- The npm bulk-advisory preflight found Mammoth `1.10.0` affected by the published DOCX directory-traversal advisory. Pinned the patched OSS release `1.12.0`, retained the archive complexity preflight, and reran real PDF and DOCX extraction successfully. The follow-up production dependency scan covered 304 package names and returned zero advisories.
- Added the local PDF.js standard-font path so searchable PDF extraction no longer falls back with a missing-font-data warning. The assets remain package-local and no runtime CDN was introduced.
- Focused proof is green for source-run fail-closed contracts, artifact build, real searchable-PDF extraction, real DOCX extraction, archive-complexity rejection, upload cleanup, and `git diff --check`. Worker source-capture persistence, atomic IDI import proof, Google orphan cleanup, the full suite, Codex-browser proof, final review, commit, and deployment remain open.

## 2026-07-14 — checkpoint 14: atomic IDI evidence and durable manual capture

- Serialized canonical IDI writes through the per-estate Durable Object guard. Concurrent different reports now produce exactly one verified winner and one conflict; paid lookup reserves the same guard before vendor spend, and only canonical verified imports can contribute Discovery facts.
- Replaced the crash-prone pending overwrite with a bounded staging record. The previous verified report stays active during replacement, a commit-to-canonical-write interruption recovers from the same report without a second logical import, and every staging cleanup path requires delete readback before returning success.
- Paid results now move through canonical staging, import-guard commit, canonical readback, paid-lock completion, and final approval in order. Ambiguous provider results, failed canonical writes, failed lock completion, transient guard status, and stale reservations remain blocked without exposing contacts or authorizing another charge.
- Contact review decisions persist against the exact canonical report revision with verified readback and reload. Replacements cannot inherit decisions from the superseded content hash, and forged browser reviewer identity is ignored.
- Made Save Capture a durable Discovery File write. The Worker binds it to the selected estate, strips all client IDI proof, ignores forged actor attribution, applies the 30-day retention policy, returns both persistence and top-level verified readback, and stores a source-only record that invalidates an older generated dossier until Discovery reruns.
- Independent hostile review reports no remaining P1/P2 in the IDI/source-integrity lane. Worker build, the complete S38 upload contract, IDI reload, source-run, route-auth, the full artifact suite, and `git diff --check` are green. Google orphan cleanup, final full-suite rerun after that lane, Codex-browser proof, dedicated final review, commit, and deployment remain open.

## 2026-07-15 — checkpoint 15: Google artifact lifecycle and integrated preflight

- Serialized each Google Drive packet delivery by a stable artifact marker. Retries reconcile a prior successful or interrupted attempt, verify the selected folder, byte size, private app properties, and SHA-256, and refuse a second upload while cleanup or reservation state is ambiguous.
- Made OCR temporary conversion fail closed. HeirRight verifies the private attempt marker before reading Google Docs text, then withholds that text until DELETE plus bounded GET readback proves the temporary Drive file is gone; an uncertain delete blocks the retry instead of creating another copy.
- Added executable lifecycle mocks for concurrent export, stale-reservation recovery, wrong-folder and wrong-size rollback, internal-receipt failure, failed cleanup, OCR marker validation, and token/file/PDF/text non-leakage. The complete artifact suite, forced workspace build, forced workspace tests, Worker validation, and diff integrity all pass.
- Repeated the production dependency preflight across 304 package names with zero npm advisories. Web Awesome Free and AG Grid Community resolve under MIT, Mammoth under BSD-2-Clause, JSZip is used under its MIT option, and the one package reported as unknown (`khroma`) ships an MIT license file. No Enterprise, Pro, runtime-CDN, or direct sunset-Shoelace dependency was introduced.
- A real Codex-browser baseline proved Dashboard-first routing, cream-theme contrast, collapsible navigation and theme continuity, AG Grid keyboard opening, whole-row document opening, and the full-height mobile contextual sheet with Escape focus restoration. A fresh hostile UI audit then found evidence-stage overstatement, an empty-selection queue fallback, and four keyboard/mobile recovery gaps; those findings are being repaired before the upload E2E and release.

## 2026-07-15 — checkpoint 16: canonical Discovery serialization and Tax Collector fetch boundary

- Replaced overwrite-in-place Discovery File writes with immutable revision staging plus a per-estate Durable Object pointer. The prior verified revision remains canonical during a write; exact content-hash and revision readback must pass before commit, and a corrupted active revision rolls back to the previous verified pointer.
- Made external source runs fail closed across the Worker, Vercel handler, and local server. A failed reservation, stage readback, commit, or canonical readback returns `503` without the unverified dossier or source facts, so the browser cannot apply an output that shared storage did not verify.
- Restricted direct Tax Collector retrieval to explicitly approved HTTPS origins. Every redirect and final URL is revalidated, requests use a bounded timeout and manual redirect loop, response bodies stream under a hard byte cap, only HTML/XHTML is accepted, and upstream error bodies never enter the API result.
- Adversarial contracts now cover a failed staged readback retaining the prior file, deterministic same-estate write contention, corrupted-active recovery, off-origin input and redirects, unsafe content type, oversized streaming content, timeout, and error-body non-disclosure. After the shell lane corrected its independent Case Journey disposition mismatch, the complete `@ple/artifact` command passed with Worker/type builds plus source-run, Tax Collector, IDI, Google lifecycle, packet-reference, auth-route, shell, Document Prep, and product-loop gates.

## 2026-07-15 — checkpoint 17: merged integrity repairs and production-shaped gate

- Closed every P2 from the first dedicated review without expanding product scope. Canonical stop rules now block source spend, upload, packet generation, approval, queue, ChatGPT handoff, and Google delivery; estate state uses the exact stable estate ID with an all-active/archived/deleted ambiguity guard; IDI reports require owner-and-address subject binding before canonical commit; and Property Appraiser completion requires verified owner, address, folio, source URL, persistence, and readback evidence.
- Bound generated packets and Google delivery to the exact estate, workflow, active packet revision, signed-in actor, current durable approval, and canonical artifact readback. The Worker verifies those facts before token refresh or Drive access. Document and packet controls resolve only same-origin, allowlisted HeirRight artifact routes with one exact identity parameter.
- Closed the UI/control review findings: Hot status cannot complete Handoff, whole document rows remain select-only, explicit Open/Download actions live in the contextual rail, action failures are visible and retryable, mobile background content is inert, keyboard shortcuts stay contained, close labels are contextual, and focus restoration is immediate.
- The settled merged tree passes the complete artifact suite, Worker validation, forced monorepo build/test/lint, 227-button inventory, real searchable-PDF and DOCX extraction, Google lifecycle adversarial tests, and `git diff --check`. The explicit production build and Vercel production build emit no source maps; the Worker deployment dry-run passes; public static output contains no operator runtime artifacts, runtime CDN imports, AG Grid Enterprise, or Web Awesome Pro; the npm bulk endpoint reports zero advisories across 304 production packages; and 296 production license entries contain no forbidden licenses.
- Three final read-only reviews are active over core/security, UI/design/control, and release/privacy. The Codex in-app browser registry currently returns no available browser surface, so the required real local upload and responsive interaction pass remains open; Chrome and standalone Playwright are not being substituted.

## 2026-07-15 — checkpoint 18: settled merged tree and live intake replay

- Closed the remaining final-review findings without expanding product scope. Every Document Prep rail tab now short-circuits Move On estates to truthful disposition context and safe next-estate navigation, and async source, IDI/contact, packet, ChatGPT Work, Google, deal-status, and document events remain attributed to the estate that initiated the operation even if the operator changes selection before completion.
- Replayed fresh searchable PDF and DOCX uploads through the real local artifact server and Wrangler Worker after restarting both from current source. Each file passed verified attachment readback, native extraction, owner-and-address subject matching, exact-estate candidate binding, sanitized canonical reload, and a verified persisted automatic Discovery source run; the intentionally mislabeled invalid PDF failed closed with `415 supporting_document_type_mismatch`.
- The settled tree passes the complete artifact suite, forced monorepo build/test/lint, Worker validation, 229-button control inventory, and `git diff --check`. The final production artifact build, Worker deployment dry-run, and Vercel production build also pass; emitted production output contains no source maps, AG Grid Enterprise, Web Awesome Pro, or public operator artifacts, and application source contains no runtime external asset path.
- The dedicated final merged-tree review found no remaining P1/P2 across Move On stops, exact-estate attribution, IDI subject binding, packet approval, controlled handoff, privacy/auth, OSS boundaries, controls, or the approved design canon. The Codex in-app browser registry still returns no available surface, so the required local computer-use upload/responsive pass, commit, exact-commit deployment, and live production audit remain open; Chrome and standalone Playwright have not been substituted.

## 2026-07-15 — checkpoint 19: approval continuity and exclusive Google handoff

- Replaced the generic-state approval mirror with actor-bound durable status hydration. The approving operator recovers the exact current packet approval after reload or workflow switching; a teammate receives an unapproved state without another operator's details, and a replacement packet revision invalidates the prior display approval.
- Made the dedicated approved-packet Google Workspace route the only live Google delivery path. Legacy export and Closing export routes stop before packet creation, readback-evidence GET remains strictly read-only, controlled-test exports require a configured administrator, and malformed boolean/action controls fail closed before proxying.
- Added adversarial regressions for generic approval forgery, same-actor reload, teammate isolation, replacement invalidation, array/number action coercion, numeric/string export-control smuggling, legacy Google routes, and read-only evidence routes. An independent current-tree re-review reports no remaining P0-P2 in this lane.
- The exact current tree passes the complete `@ple/artifact` suite with 229 button contracts, Worker and type builds, real searchable-PDF and DOCX extraction-to-Worker readback, Google lifecycle tests, the explicit production artifact build, no production source maps, and `git diff --check`. Codex in-app browser interaction proof, commit, exact-commit deployment, and live production audit remain release gates; Browserbase disposable sessions remain blocked by `402 Payment Required`.

## 2026-07-15 — checkpoint 20: Estates grid geometry closeout

- Rebuilt the Estates surface around explicit header, filter, meta, and grid areas so a hidden filter row cannot consume the flexible track or strand pagination above the bottom of the rounded workbench. The grid reserves token-based clearance for the Search & Commands overlay instead of hardcoding a viewport height.
- Centered AG Grid Community pagination inside the workbench, removed its hard top rule, reset the translation on mobile, and visually suppressed native scrollbar tracks and thumbs while retaining the overflow viewports and AG Grid synchronization elements required for wheel, trackpad, touch, and keyboard scrolling.
- Removed the grid's opacity-zero entrance state and hover lift so records stay visible when motion is throttled and controls do not jump. The focused Document Prep/grid contract and `git diff --check` pass from the integrated tree.
- The combined local grid Playwright path passed Estates and Queue twice, then timed out at an unrelated empty Admin-audit fixture before focusing its first row. This is recorded as an open integrated-E2E investigation rather than misreported as a complete browser pass; the required Codex-browser matrix remains the release gate.

## 2026-07-15 — checkpoint 21: approved shell-annotation closeout

- Extended desktop content through the transparent command-overlay track and applied the requested 10px inset at the owning content gutter, so the rounded workbench and its contents move together without a hardcoded viewport height. Search & Commands now uses the full-width bottom track as its fine-pointer hover/focus zone, reveals a compact right-aligned chip, and expands horizontally into one outer pill with a transparent, borderless form; the stale joined-panel rules that produced the cup silhouette are gone.
- Kept keyboard and touch recovery explicit: activation focuses the command input, Escape and close restore the launcher when appropriate, opening the Case Journey closes the command surface without stealing rail focus, reduced-motion remains supported, and coarse-pointer layouts expose the chip without requiring hover.
- Replaced placeholder shields with the real self-hosted transparent HeirRight mark, wired PNG copying and `image/png` delivery into the artifact build/server, moved the singleton Dark/Cream/System control into Settings → Preferences with a rerender remount event, and replaced the Case Journey glyph with the 23px semantic journey alias.
- Applied the approved graphite capsule material to native actions, Web Awesome base parts, and action links while keeping navigation, account, logo, editorial rows, menus, grid rows, and backdrops on their own semantics. Exactly one navigation item receives the active plate; the LB account surface is borderless; the footer uses the approved fading one-pixel ruler; broad glows and hover lifts are absent.
- Fresh focused proof passes: the shell contract, artifact build, static-build privacy contract, and `git diff --check`. The real responsive and input-method verification remains open in the Codex in-app browser and no commit or deployment has occurred.

## 2026-07-15 — checkpoint 22: blue interaction system and Codex-browser geometry proof

- Replaced the former cream/gold interaction accent with HeirRight/iOS blue across both graphite and cream themes. Shared action, border, focus, selected, icon, and glow tokens now resolve through the blue system; semantic warning and error colors remain reserved for status meaning.
- Made icon-only controls bare marks. Resting, hover, and keyboard-focus states cannot add a fill, border, or box shadow; the only hover/focus treatment is a tight blue glyph glow. The mobile primary command follows this contract when its label collapses below 820px, while the one active navigation item keeps its selected plate to communicate location.
- Applied the final shell annotations: the search shortcut badge is transparent, the header search and Import split use the requested optical offsets, the Case Journey mark is 27px, the account control is borderless, and the footer ruler is removed. The user’s later no-line instruction supersedes checkpoint 21’s fading-ruler state.
- Settled Estates pagination at the horizontal center with a 32px desktop bottom gap inside the rounded workbench. AG Grid scrollbar tracks and thumbs remain visually hidden without removing overflow or synchronization elements; at 390px the compact pager stays on one line after wheel scrolling reaches the bottom.
- The real Codex in-app browser passed dark and cream at 1615×876, 1173×994, 1440×900, 1280×720, 768×1024, and 390×844 with zero body overflow, one selected navigation item, transparent search badge, hidden scrollbars, and a 27×27 Case Journey glyph. The 1280px pager measured a zero-pixel center delta and a 32px bottom gap. At 768px the collapsed command measured 44×44 with zero padding, transparent fill and border, no shadow, and blue glyph color.
- Codex-browser interaction proof covered command open/focus/Escape restoration, Case Journey open/Escape restoration, Settings theme persistence and singleton remounting, and real wheel scrolling through the mobile grid. The Browser CUA pointer-move call did not synthesize CSS `:hover`; the shared hover/focus selector was therefore exercised through true `:focus-visible`, which measured transparent fill and border, no shadow, a blue glyph, and the soft radial blue glow. The focused shell, grid, component-runtime, production-build privacy, and `git diff --check` gates pass from the rebuilt source.

## 2026-07-15 — checkpoint 23: reviewed IDI contacts and exact packet-replacement replay

- Added visible Accept, Promote, and Reject actions to the unified Document Prep Automation rail. Each action revalidates the exact selected estate, active report revision, candidate membership, and current review status before using the existing verified Worker write. A real Codex-browser click accepted Mateo Rivera for the Alicia fixture, changed the rail to `Decisions saved / Accepted`, and canonical readback recorded the exact attachment-bound decision.
- Corrected inline Discovery recovery so Property Appraiser, tax, deed, obituary, contact, and generic source blockers identify the source phase that actually failed and offer `Retry Discovery`. A saved IDI report is no longer blamed for an unrelated source blocker, and operators are not told to choose another file when the valid report is already canonical.
- Closed a packet-generation defect found by the exact replay: accepted or promoted IDI contact facts now populate the completed report contact matrix when no approved skip-trace profile exists. The reviewed contact keeps the relationship-as-research-lead caution but carries no generic contact-review placeholder flags. The S38 IDI contract now proves this from canonical review through `completedLeadReport.contactPlaceholders`.
- Fixed joined-field extraction so `Phone:`, `Email:`, `Address:`, `Relationship:`, and status labels cannot leak into a candidate name when PDF extraction joins lines or a DOCX uses hyphen-delimited fields. Both real fixtures now extract exactly `Mateo Rivera`, with the expected `305-555-0142` phone and source locator.
- Replayed the complete local backend chain on `crm:csv:S38-ALICIA-E2E-20260715-04`. The searchable PDF persisted as `supporting-1784133279181-24ae9cdb5904b27b` with SHA-256 `2531e549b9f2995feb57af7392b338a38c5a11639cd028445f3adf49d005521e`, extracted by `pdf_text`, passed exact-estate subject binding, required and saved contact review, generated packet revision 1 as `packet-1784133279874-6a35e2b28091a62a`, and read back a valid 30,950-byte PDF.
- Replaced that report with searchable DOCX `supporting-1784133279909-054c65e087e35abd`, SHA-256 `9f0d27e34e03f4c0b2ce1176330bb455dad347090ee1863f9899c0fe2580e01f`. Replacement required and preserved the descriptive administrator reason, invalidated the PDF-era contact decision, required a fresh Mateo review, regenerated canonical Discovery, and produced packet revision 2 as `packet-1784133280663-a5cd12b1d7e6d958` with a verified 30,958-byte readback.
- Rerun replacement matches the locked product rule: canonical Discovery contains exactly one active packet, revision 2, and no longer treats revision 1 as active. Google Workspace returned connected `false` with no destination, while local packet completion stayed green; the intended next action remains guided Google OAuth setup rather than a packet blocker.

## 2026-07-15 — checkpoint 24: exact browser estate completion and final control repairs

- Added a durable browser-facing fixture as `crm:csv:S38-ALICIA-UI-20260715-05`, then opened that exact AG Grid row from its title cell with Enter. The visible Document Prep surface stayed bound to the exact estate, showed the accepted `Mateo Rivera` review decision, and completed the automatic Discovery timeline through `Ready for review` at 100%.
- The first visible completion run found a real packet-audit defect after the PDF itself had already passed readback: `linkGeneratedDocPrepPackets` reacquired the estate documents after taking a mutable flow-state reference, which allowed record normalization to replace the object underneath the audit update. The linker now resolves one stable document array before the mutable flow state and reuses it through the audit write.
- Replaying the visible `Run Discovery` control produced active packet `packet-1784134027364-acce6316296c12ae`. The artifact route returned a verified 30,856-byte, 13-page PDF, the automation rail showed every stage complete, and the Completion tab exposed packet review, download, ChatGPT Work, and optional Google setup without treating Google as a local-completion dependency.
- Live completion-action review removed two false popup claims. `Open current packet` now revalidates the exact current unexpired packet and navigates the current browser tab to its same-origin verified PDF; the download remains a separate verified action. `Continue in ChatGPT Work` copies the sanitized exact-packet brief before same-tab navigation and no longer claims a discarded popup opened. The copied 609-byte brief contains the estate summary and verified packet route while excluding raw IDI text, the accepted phone, content hashes, credentials, bearer values, and tokens.
- The visible optional-Google action routed to Settings and showed `Google login setup needed`, the unconfigured OAuth guidance, and the still-available local packet. Settings → Preferences retained exactly one Dark/Cream/System control across rerenders and preserved the chosen cream theme across navigation.
- Closed the final repo-wide blue-accent audit: cream selected surfaces, legacy accent/focus fallbacks, checked controls, settings ranges, and AG Grid pager glyph states now use the HeirRight/iOS-blue token family. Neutral cream surfaces and semantic warning amber remain intentionally semantic. Unselected icon-only controls keep zero fill, border, or box shadow; the glyph-only hover/focus contract uses the shared tight blue glow, while the one active navigation item remains the only selected plate.
- The Codex Browser later stopped dispatching both pointer and keyboard activation for ordinary controls, including the unchanged Queue navigation button, while DOM inspection, screenshots, direct navigation, and packet readback remained available. That isolates the late no-op clicks from the packet implementation, but the exact packet-open, ChatGPT URL, Back-restoration, download, and Queue-hover interactions remain explicit final browser replay gates before release rather than being represented as complete.

## 2026-07-15 — checkpoint 25: annotation closure and truthful release gates

- Closed the final visible annotation set without adding product behavior. Document Prep now keeps only the load-bearing page and section titles, so the redundant `Document Prep`, `First step`, and `Estate file` body eyebrows are gone. Web Awesome action glyphs use the start slot; the real Codex browser measured a 44px `Run Discovery again` control with a zero-pixel icon-to-label center delta.
- Flattened the Dashboard's repeated uppercase kicker-over-heading treatment while preserving its estate, disposition, Case Journey, attention, files, and update hierarchy. The seven lifecycle controls remain fully accessible but render as bare status marks: the Codex browser measured transparent button and marker backgrounds, zero borders, and no shadows for every step.
- Removed opacity-zero and blur-gated entrance frames from current Estates, routed views, Document Prep, inline recovery, rail failures, account/menu surfaces, walkthroughs, and page rows. Motion is transform-only, so a throttled or failed animation cannot blank operator content. Source contracts reject any return of the affected opacity-zero keyframes.
- Repaired the release lint gate itself. `pnpm turbo run lint --force` now executes real uncached work: TypeScript `--noEmit` checks for Worker and shared types plus syntax checks across 100 artifact JavaScript modules. The prior no-op lint result is not counted as evidence.
- Fresh focused proof passes for the shell, Document Prep, unified-rail runtime, artifact build, real lint graph, and `git diff --check`. A real Codex-browser navigation click opened Dashboard, and settled 1280×720 screenshots confirmed the cleaned Document Prep hierarchy, direct Dashboard headings, blue action system, bare lifecycle markers, and zero page overflow. The independent full release gate and final hostile review are still active; no commit or deployment has occurred yet.

## 2026-07-15 — checkpoint 26: contextual Estate action and footer alignment

- Applied the newest explicit annotation over checkpoint 20's older metadata geometry. Estates now owns only explicit header, optional-filter, and flexible-grid areas; its idle result/selection strip is gone. Queue keeps its own metadata row because that surface still needs queue scope and export state.
- Reused the existing selected-estate Queue action without adding another workflow. It remains absent at zero selections, appears as a 44px pill for click or Space-key selection, names singular and plural scope exactly, disappears again after deselection, and keeps success or failure feedback visible outside the hidden assist wrapper.
- A real Codex-browser replay found that the Queue write itself succeeded while the confirmation targeted the detached pre-rerender grid. The action now resolves the live Estates root after dispatch and ignores selection-restoration callbacks that did not change scope. Replaying the same click produced `1 estate added to Queue.` with the `ready` tone, and the exact selected estate was present in Queue.
- At the annotated 1350×994 viewport, the selected-estate KPI strip computed to `margin-left: 15px`, `margin-bottom: 5px`, and `align-self: end`; the integration status strip computed to the same 5px bottom offset and both rectangles ended at pixel 989. The collapsed LB avatar computed to `margin-left: 2px` and `margin-bottom: -13px`. The page had zero horizontal or vertical overflow.
- Responsive boundary proof covered 1121/1120, 820/819, expanded navigation, and 390×844. Both desktop footer clusters stayed pixel-aligned at 1121 and after sidebar expansion; the integration strip hid at its existing 1120 breakpoint; desktop offsets applied at 820 and cleanly reset at 819 and 390. At 390 the contextual action remained a readable 304×44 control with its feedback below it and zero body overflow.
- The Estates pager remained at the exact horizontal center of the rounded workbench, and the main content measured the requested responsive 860.4px height at 1350×994 without a hardcoded viewport height. Evidence: `screenshots/2026-07-15-s38-contextual-estate-action-1350x994.png` and `screenshots/2026-07-15-s38-footer-alignment-1350x994.png`.
- Focused proof passed: `node apps/artifact/test/s38-shell-contract.test.mjs`, `node apps/artifact/test/s38-doc-prep-contract.test.mjs`, `node apps/artifact/test/s38-unified-rail-runtime.test.mjs`, fresh artifact builds, `git diff --check`, the Community-grid Playwright path, and the S37 estate-to-Queue product-loop path. The dedicated hostile review reran all ten S38 contracts and reported no P0-P3 finding on the current three-area contract.

## 2026-07-15 — checkpoint 27: fresh packet rerun and Codex-browser ChatGPT recovery

- Replayed the exact Alicia browser fixture from the visible `Run Discovery again` control with a reviewed replacement note. The Web Awesome submit element rendered but did not submit its parent form in the Codex browser, so the rerun control now binds the same guarded submit routine to both the native form event and the custom-element click. The live replay advanced to `Discovery running`, returned to `Ready for review`, and replaced the active local packet.
- The fresh Completion rail exposed current-packet open/download, optional Google setup, and `Continue in ChatGPT Work`. The packet remained complete while Google was unconfigured, preserving the locked local-first completion rule.
- Real browser use exposed a second capability boundary: the Codex browser provides neither a writable Clipboard API nor the legacy copy command. The handoff no longer misreports that as a packet failure. It first tries the modern clipboard, falls through after a denied write, then presents the same sanitized 609-byte brief in a focused read-only field when no copy API exists.
- Using real computer input, the selected brief accepted `⌘C`, `Continue in ChatGPT Work` navigated the same tab to `https://chatgpt.com/`, and browser Back restored `crm:csv:S38-ALICIA-UI-20260715-05`, the Document Prep view, and the verified active packet. Escape closes the recovery dialog and returns focus to the visible Case Journey trigger when its source rail has closed.
- The denial-path regression now proves three distinct states: an expired packet stays blocked with no ChatGPT navigation; a current packet with blocked clipboard access exposes the selected sanitized brief without navigating; and the explicit continuation produces exactly one same-tab navigation. A second pass keeps the automatic clipboard path covered when write access is available.
- The adversarial rapid-activation replay found and closed one final edge case: a second click could retarget the newly opened modal backdrop and dismiss it immediately. The manual-copy recovery now requires its explicit Cancel, close, Escape, or Continue controls; cancellation restores truthful local-packet status and returns focus to the visible Case Journey trigger rather than a rail button that is still visible only during its close transition.
- Focused proof passed after the repairs: the S38 Document Prep and shell contracts, a fresh artifact build, `git diff --check`, and the full verified-PDF upload/automatic-Discovery/rerun Playwright scenario. Evidence: `screenshots/2026-07-15-s38-chatgpt-manual-copy-1350x994.png`.

## 2026-07-15 — checkpoint 28: hostile handoff repair and exact-state browser proof

- A fresh hostile review found three concrete handoff defects before tree freeze: the footer `Cancel` control was not wired, the manual-copy dialog could continue against a newly selected estate or a replaced/expired packet, and closing the rail could attempt to return focus to an action inside the closing rail. All three paths now share the same exact-estate, current-packet, and visible-focus boundary as the primary completion flow.
- The handoff snapshot now records the exact estate ID, estate label, workflow ID, packet artifact ID, packet revision, and sanitized brief. Rendering never falls back to the current selection. Continue re-runs the canonical stop-rule and current-unexpired-packet checks, compares both artifact ID and revision with the captured packet, keeps the dialog open with an inline alert on failure, and performs no navigation until that validation succeeds.
- Both modal close controls now use the shared close routine. Focus returns to the rail invoker only while the rail is genuinely open; otherwise it returns to the visible `Open Case Journey` shell trigger. The focused contract covers missing-estate, selection drift, packet replacement/expiry, inline failure, all close controls, and the closed-rail focus fallback.
- Fresh automated proof passed: `node apps/artifact/test/s38-doc-prep-contract.test.mjs`; `pnpm --filter @ple/artifact build`; `git diff --check`; and `pnpm exec playwright test apps/artifact/e2e/s38-doc-prep-and-grid.spec.mjs --grep "a verified PDF binds to the selected estate" --workers=1` (`1 passed`, 10.3 seconds). The E2E replay also expires the packet while the manual dialog is open, proves the inline error and zero navigation, restores the test clock, and then proves the current-packet continuation.
- The real Codex browser independently exercised the repaired footer `Cancel`; the dialog count fell to zero, status read `ChatGPT Work handoff canceled. The verified local packet remains ready.`, and focus landed on `#s38OpenRail` with `aria-label="Open Case Journey"` while the rail was closed. Reopening Completion and activating the handoff exposed exactly one dialog and one `Continue in ChatGPT Work` control.
- Continuing navigated the same controlled tab to `https://chatgpt.com/`. Browser Back restored `http://127.0.0.1:4188/?view=dossiers&s38=clipboard-fallback`, the `Estate of Alicia Rivera` state, the `Discovery packet verified` result, and `Revision 2`. There were zero application-owned console errors; the one logged error belonged to ChatGPT's external Google/FedCM token request and did not originate from HeirRight.

## 2026-07-15 — checkpoint 29: release-inventory repair

- The first frozen-tree release gate correctly failed before commit: the new manual ChatGPT continuation was wired and worked in the browser, but its conditional `disabled` interpolation directly followed `data-continue-chatgpt-work`, so the static 229-control inventory could not recognize the action attribute. The prior focused-browser result remains valid, but the integrated tree is not considered green until the full gate is repeated.
- Added the literal delimiter owned by the generated opening tag without changing runtime behavior. A missing estate still renders the native `disabled` attribute, while a current estate renders the same enabled control and remains bound to the existing exact-estate/current-packet listener.
- The parallel hostile review reported no other P0-P3 finding. It reconfirmed that `test-results/` and `apps/artifact/test-results/` are generated, unignored evidence directories; release staging will therefore use the independently audited explicit S38 allowlist and assert that neither result directory, root sprint brief, build output, dependency directory, nor deployment cache entered the index.

## 2026-07-15 — checkpoint 30: repaired release tree and Vercel trace hygiene

- Repeated the static control gate after the opening-tag delimiter repair. `node apps/artifact/test/s37-control-inventory.test.mjs` now inventories 233 controls and passes; the S38 Document Prep contract, explicit production artifact build, `git diff --check`, and the verified-PDF upload-to-automatic-Discovery Playwright path also pass (`1 passed`, 9.4 seconds).
- Repeated the exact recovery path in the Codex in-app browser after rebuilding. The Alicia Document Prep state retained one verified packet and one enabled `Continue in ChatGPT Work` control; footer `Cancel` removed the sole modal, left zero dialogs, and returned focus to `#s38OpenRail` with accessible name `Open Case Journey` while preserving `?view=dossiers&s38=clipboard-fallback`.
- The release gate found that an earlier local Vercel trace had copied development `app.js.map` and `app.css.map` files into four serverless bundles even though the current static production output was clean. Both supported Vercel project-root configurations now exclude only browser `.map` files from API function traces; development builds still emit their local external maps.
- Adversarial build proof first generated both development maps, then ran the exact Vercel production build. The explicit production artifact removed both source maps, Vercel completed successfully, and a recursive scan of `.vercel/output` found zero `.map` files. The focused static-build/privacy test now locks both exclusion contracts.
- This repaired source tree is frozen for the complete uncached release gate and a second dedicated read-only hostile review. No files are staged, committed, pushed, or deployed at this checkpoint.

## 2026-07-15 — checkpoint 31: reviewed-none tax capture repair

- Reproduced the production external-source failure with the UI-compatible free-text Tax Collector field. Cloudflare returned Worker `1101`; the live trace identified `dossier.taxHistory.unpaidYears.value?.join is not a function` while building the completed report. The prior verified Discovery revision remained canonical because the failed run never passed persistence.
- Normalized the operator's reviewed-none value to the dossier's existing `number[]` contract. A reviewed `None found` result now persists as an explicit empty list, while text containing real years continues through the existing Tax Collector parser as numeric years. Source capture no longer leaks a scalar into a typed claim.
- Hardened both completed-report renderers against legacy or malformed scalar values. The operator report renders a verified empty list as `None found`, and both report paths safely stringify defensive fallback values instead of calling `.join()` on unknown input.
- Added local and full Worker route regressions that submit the exact free-text reviewed-none shape, require HTTP `200`, verified canonical readback, and a persisted empty-array fact. A hostile review then caught over-broad normalization before commit; `unknown`, `not checked`, `see receipt`, and malformed `202` now remain missing and review-required, with regressions at both the capture helper and canonical dossier boundary.
- Replayed the production-shaped payload through the real local HTTP artifact and Worker processes. The route returned HTTP `200`, canonical persistence `verified`, source facts `[null, []]`, and dossier unpaid years `[]` without a Worker exception. The complete workspace test graph, real artifact lint across 101 JavaScript modules, TypeScript no-emit checks, explicit production build, and `git diff --check` all pass from the repaired delta.
- The post-repair read-only hostile review reports no remaining P0-P3 findings across type, persistence, rendering, security, or workflow contracts. Commit and deployment now proceed from this exact reviewed tree; the same payload must still pass against the deployed Worker revision before release closeout.

## 2026-07-15 — checkpoint 32: provider-truth and final annotation freeze

- Reconciled every Settings surface with the real Browserbase state without changing a backend route, response shape, or workflow contract. Missing access now reads `Setup required`/`not connected`; configured review-only access reports the verified HTTP `402` billing blocker; a future existing-contract `mode: "live"` status clears the incident copy. The live fixture is a synthetic compatibility check, not a claim that either current status builder can auto-verify recovery.
- Removed the last cross-surface contradictions in Integrations, connection rows, Sources, Tax Collector, and Vital/Obituary. Missing functions no longer inherit saved/ready copy, while configured Browserbase-backed functions remain review-blocked until billing is restored and one disposable session is successfully verified.
- Added real-browser regressions for configured-review, real-shaped missing setup, and future-live UI compatibility. `node apps/artifact/test/s34-app-owned-readiness.test.mjs`, the six-test S38 shell/Settings Playwright file, app syntax, and `git diff --check` pass. The external Expect runner's Claude backend was unauthenticated; its Codex retry exited without structured findings, so it is not counted in place of the passing Playwright and Codex-browser evidence.
- Replayed the newest annotations in the real Codex in-app browser at 1350×994. Estates renders no idle result/selection strip; `Add estate to Queue` is absent at zero and appears after selecting the exact public-source estate. The KPI strip and connection strip both ended at pixel 989 with `margin-bottom: 5px`; the KPI kept `margin-left: 15px`; the LB avatar kept `margin-left: 2px`, `margin-bottom: -13px`, transparent background, zero border, and no shadow.
- A dedicated read-only Browserbase contract review reports no remaining P0-P2 finding in this delta. Automatic provider recovery still requires a later health-status change after billing is restored; that expansion is outside the locked S38 interface contract. Production remains on the prior runtime until this reviewed repair is committed and deployed.

## 2026-07-15 — checkpoint 33: labeled-action row alignment

- Applied the newest approved button annotation through one `0.25rem` shell token. Native labeled actions, Web Awesome action hosts, action links, and both halves of the Import split control receive the same four-pixel row offset; icon-only controls, navigation, tabs, menus, editorial rows, and grid glyph controls retain their existing zero-offset geometry.
- Real Codex-browser proof at the requested 1649×984 viewport measured `margin-top: 4px` on `#crmImportSingle`, `#crmImportBatchToggle`, the Estates `Filters 0` action, and the `wa-button[data-run-discovery]` host. The Import label and arrow shared center Y 49.8px; the Web Awesome discovery icon and label shared center Y 378.3px. `#s38OpenRail` remained at `margin-top: 0px`, preserving the bare icon-only contract.
- The focused shell contract, fresh artifact build, and all six S38 shell/Settings browser tests pass after the alignment change. The test now locks the shared token, each labeled-action primitive, aligned Import halves, and the icon-only exclusion.
- The disposable `/private/tmp` worktree disappeared after the earlier full test pass. The reviewed source remained safe on pushed commit `12548e6`; Git reported only a prunable missing worktree record. That record was pruned and the same `feat/idi-report-intake-demo` branch was recreated from `12548e6` at the same path. No canonical source file was checked out, reset, staged, or changed by the recovery.

## 2026-07-15 — checkpoint 34: action semantics and final-review hardening

- Closed the dedicated design review without adding a new surface. Bare icon controls and unselected navigation keep transparent fill, zero border, and zero shadow, but keyboard focus now has the explicit two-pixel HeirRight-blue ring that the previous transparent outline suppressed. AG Grid Community pagination/header glyph states retain plate-free hover feedback, and the existing Queue row action keeps its visible keyboard ring.
- Made the stable Case Journey trigger describe the surface it actually opens. Desktop exposes a nonmodal disclosure with `aria-controls` and synchronized `aria-expanded`; the same trigger adds `aria-haspopup="dialog"` only while the responsive rail is a full-height mobile sheet. Open/close and breakpoint changes update the contract without duplicating the trigger or rail state.
- Preserved the intentional Document Prep empty-state CTA spacing while applying the global four-pixel labeled-action baseline. The shared Web Awesome host rule now accepts a component-owned spacing override, so the empty action remains twelve pixels from its copy while ordinary action rows retain the approved four-pixel optical shift.
- Closed the two low-severity security findings from the release review. Anonymous login and session responses no longer disclose exact approved or administrator email addresses; ordinary sessions retain the stable empty-list response shape and access-management sessions keep the existing exact list. Node/Vercel and Worker bearer, session-HMAC, Google-state, and Podio-state checks now compare fixed-size SHA-256 digests through `timingSafeEqual`.
- Hostile runtime regressions reject same-length, truncated, and extended bearer/session near-misses plus tampered Podio state and state-cookie signatures. Focused route-auth, production-route, Podio OAuth, Worker build, artifact lint, shell contract, and `git diff --check` gates pass.
- Fresh Codex-browser proof reconfirmed the requested 1649×984 alignment: both Import halves use `4px` and share center Y 49.8px; Filters uses `4px`; Case Journey remains `0px`; the Document Prep Web Awesome icon and label share center Y 566.9px. Desktop rail disclosure opens and closes with synchronized state and focus restoration. At 390×844 the trigger reports the mobile dialog contract, both compact icon actions remain at `0px`, and the page has no horizontal overflow.
- The first expanded E2E run correctly rejected focus assertions that used programmatic focus, which does not activate `:focus-visible`. The tests now establish keyboard modality before checking the explicit ring and separately prove plate-free AG Grid glyph hover. The repaired Dashboard keyboard path and full Community-grid flow pass; the complete release suite is being repeated from this repaired tree before commit.

## 2026-07-15 — checkpoint 35: exact-tree release gate and hostile closeout

- Closed the last hostile-review privacy finding in the authenticated access bootstrap. Ordinary Google operators and temporary exact-IP sessions now receive approved domains with an empty exact-email list; exact Google administrators, local auth-off development, and the private server bearer retain the full readback required for access administration. Focused regressions cover all four paths without changing the route shape used by the client.
- Repeated the complete release graph after the repair. The forced monorepo build passed 3/3 uncached tasks, the forced test graph passed 5/5 tasks, lint passed 4/4 tasks across 102 application modules, Worker validation produced 61 facts, the Browserbase function contract passed, and the complete Playwright matrix passed 17/17 in 1.1 minutes. This includes real searchable-PDF and DOCX extraction, exact-estate binding, automatic Discovery, inline recovery, active-packet replacement, Community-grid keyboard behavior, optional Google recovery, ChatGPT Work handoff, and all 233 executable controls.
- Reconfirmed the rebuilt interface in the real Codex in-app browser. At 1649×984 both Import halves computed to `margin-top: 4px` and center Y 49.78px, the Case Journey icon-only trigger remained at `0px`, the page had zero horizontal overflow, and closing the desktop rail restored focus to `#s38OpenRail`. At 390×844 the compact action remained at `0px`, the Journey trigger exposed `aria-haspopup="dialog"`, the opened sheet exposed `role="dialog"` plus `aria-modal="true"`, closing restored focus, and horizontal overflow remained zero.
- Built the explicit minified production artifact with source maps disabled, passed the static privacy allowlist, passed the Worker deployment dry-run at 4,142.74 KiB / 1,255.94 KiB gzip, and refreshed the linked `solvys/heirright-landing-demo` Vercel production output. The Vercel trace contains the new access guard, all 14 PDF.js standard fonts, no `.map` files, and no native canvas binding.
- Repeated the OSS and supply-chain preflight. The retired npm audit endpoint returns HTTP `410` with its documented migration notice; the supported bulk-advisory endpoint returned zero advisories across 304 production package names. The production license inventory contains 296 entries with no forbidden license; the one tool-reported unknown, `khroma`, ships an MIT license. Application source has no AG Grid Enterprise, Web Awesome Pro, direct Shoelace component library, or runtime CDN; Web Awesome Free's two transitive Shoelace utility packages remain its own MIT dependency chain.
- Three dedicated read-only hostile passes report no remaining P0-P3 source, workflow, security, privacy, accessibility, control-behavior, or design-canon finding. `git diff --check`, the high-confidence tracked-secret filename scan, and explicit release staging review are clean. Generated `test-results/`, build output, dependency directories, deployment caches, and the root sprint brief remain outside the staging allowlist.
- External truth remains unchanged and is not hidden by the green core flow: Browserbase disposable sessions are still blocked by HTTP `402`; Google OAuth credentials remain the intended customer setup; and `surface.heirright.com` still does not resolve while the Podio callback targets it. The Vercel production URL can support the S38 local packet workflow, but neither the custom-domain nor Podio Connect path can be called production-working until DNS and callback routing are repaired outside this code scope.
