# HeirRight S26/S27 Completion Evidence - 2026-06-29

Branch: `v1.1.0/heirright-contract-completion-s26-s27`

## Scope Reviewed

- Reviewed the S24/S25 completion evidence before starting S26/S27.
- Confirmed S24/S25 work is present on this branch through `2356220 Complete S24 S25 integration and legal routes`.
- Preserved the strict IDI Core advisory: IDI Core is expensive per run, no extra paid run was triggered, and the Asset Discovery intake path remains locked to operator import plus duplicate blocking until TP/client explicitly approves a single paid proof.
- Preserved pre-existing dirty dependency edits in `probate-lead-engine/package.json` and `probate-lead-engine/pnpm-lock.yaml`.

## S26 Human-Practical Test Result

Status: complete for product-loop testing, with external live-write blockers kept explicit.

- Estate import practical test passed on the production app alias.
- Import rail defaults closed; opening it exposes the two operator choices: import from external sources and fetch fresh leads.
- Batch import from CRM/Podio review data added estates and routed them to Discovery before Closing.
- Fresh lead generation did not fake success. It returned the exact production blocker: `Fresh public-source pulls require HEIRRIGHT_WORKER_URL or WORKER_API_URL before production can run a real lead-generation batch.`
- Doc Prep list/add workflow showed eligible estates, opened the selected client through the breadcrumb/product loop, and kept the imported estate name visible in the document prep context.
- Deal status update path moved an estate from Pre-Discovery through Warm and made Closing Docs available.
- Discovery source capture, IDI operator import, duplicate IDI blocking, and contact review were all exercised without paid IDI Core reruns.
- Outreach approval guard and integration onboarding surfaces were exercised.
- Settings showed Podio, Google Workspace, Resend, and SMS onboarding paths with fail-closed status.
- Mobile app proof passed without horizontal overflow after the export menu fix.

## S27 Product Review Result

Status: final review packet complete, with external decisions still open.

Corrected contract items:

- Automate Discovery Doc Prep: product loop, progress storage, source capture, IDI import review, contact review, and status movement are production-proved.
- Automate Closing Doc Prep: Closing flow is available after status progression, packet rows and template mapping are present, and final legal/template approval remains a signoff item.
- New website: landing, Contact, Terms, Privacy, sitemap, and metadata are production-proved on the Vercel website alias.
- SMS and email outreach automation: campaign/template approval and integration gates are present; live send remains blocked until provider/compliance approval.
- UI error-free practical pass: S26 defects found during browser testing were repaired and redeployed.
- Google Sheets and Podio integrations: fail-closed routes, dry export, onboarding, and exact live-readback blockers are proved; live write/readback remains externally blocked by missing credentials/config.

Human decisions still needed:

- Final closing-template legal/client approval.
- Outreach compliance approval for email/SMS copy and cadence.
- SMS provider decision or approved Podio-native SMS path.
- Live Podio and Google Workspace credential owner plus one controlled readback run.
- Custom domain decision if the client wants `heirright.com` moved from the current WordPress/Cloudflare surface to the Vercel site.
- Lead generation activation timing. Current scope requires enriched leads now and the capability to fetch fresh leads later; production fetch remains blocked until the worker URL is configured.

## Production Proof

- Operator app alias: `https://heirright-landing-demo.vercel.app`.
- Final app deployment: `https://heirright-landing-demo-236kpz2ui-solvys.vercel.app`.
- Final deployment id: `dpl_HxAH2GTq2gKM6gkDwYzvq8pZheAW`.
- Public website alias: `https://heirright.vercel.app`.
- Website route proof: `/`, `/terms.html`, `/privacy.html`, and `/contact.html` returned HTTP 200 with no desktop or mobile horizontal overflow.
- Contact form proof: production form returned confirmation `HR-20260629-D1FB58AE`.
- Mobile app screenshot proof: `/tmp/heirright-s26-app-mobile-final.png`.
- Website desktop screenshot proof: `/tmp/heirright-s26-site-contact-desktop.png`.
- Website mobile screenshot proof: `/tmp/heirright-s26-site-mobile.png`.

## Defects Found And Routed Back

- S23: Closed DocPrep research rail still exposed hidden controls to browser hit testing. Fixed with closed-state visibility and verified by reopening the rail for source/IDI/contact actions.
- S23: DocPrep add/list context used owner initials instead of imported estate names. Fixed by centralizing the estate label used by list rows, add modal, breadcrumb, status labels, and top-status events.
- S24: Production artifact app had local-only API coverage for fresh leads, IDI import, source capture, and contact review. Added Vercel serverless routes with fail-closed guardrails.
- S24: Fresh-lead blocked state surfaced a machine code instead of the operator blocker. Fixed UI copy selection to show the exact missing-worker blocker.
- S25/S26: Hidden export menu widened the mobile viewport. Fixed closed popover visibility and mobile right alignment.
- S26: Collapsed estate source rail could receive hit testing while closed. Fixed collapsed-state visibility.

## Validation Commands

- `cd probate-lead-engine && pnpm build` passed.
- `pnpm --filter @ple/worker test` passed with 54 facts.
- `pnpm --filter @ple/worker run:dry -- --address="20611 NW 33rd Pl, Miami Gardens, FL 33056" --owner="Fresh public-source lead"` passed.
- `pnpm --filter @ple/worker run:daily` passed and preserved review-only status for current seed volume.
- `pnpm --filter @ple/worker export:dry` passed.
- `pnpm --filter @ple/worker export:podio-live-test` failed closed before write because Podio credentials/config are missing.
- `pnpm --filter @ple/worker export:google-live-test` failed closed before write because Google Workspace credentials/config are missing.
- `pnpm --filter @ple/worker milestone:30-day` passed and reported `overallStatus: blocked` for external gates.
- `pnpm --filter @ple/artifact build` passed.
- `cd site-v2 && pnpm build` passed.
- `git diff --check` passed after the evidence update.

## API Proof

- `POST /api/leads/fresh-batch`: HTTP 200, `ok: false`, `status: blocked`, no external lookup/write, worker URL blocker visible.
- `POST /api/discovery/idi-asset-search/import`: HTTP 200, `ok: true`, `paidRun: false`, `duplicateGuard: first_import_only`.
- `POST /api/discovery/source-capture`: HTTP 200, review receipt returned.
- `POST /api/discovery/contact-candidates/qa/review`: HTTP 200, review receipt returned.

## /solvys-heir-audit Closeout

Source checked: S24/S25 completion note, S26/S27 briefs, contract completion plan, run-point automation doc, and the deal-flow checklist.

Backward: S24/S25 produced the outreach/integration gates and public website/legal routes. S26 tested the production app like an operator and found the remaining launch-friction defects. S27 converted the result into a final contract-readiness packet.

UX pass: Product-loop issues found in the practical pass were fixed and redeployed. Closed rails and popovers no longer interfere with mobile or hidden-state interaction. Operator-facing blocked states now tell the client exactly what is missing.

Forward: Next work is not another implementation sprint unless new feedback arrives. The safe next moves are credentialed Google/Podio readback, one approved IDI proof if still required, final closing-template signoff, outreach compliance approval, and optional custom-domain migration.

Alignment: Matches the corrected scope: enriched leads and doc prep automation first, fresh lead generation as a guarded capability, website/legal pages complete, outreach gated, and Google/Podio ready for controlled live proof.

Required corrections before complete:

- Live Google Sheets and Podio readback remain blocked until approved credentials/config and controlled-write approval are supplied.
- No paid IDI Core run was executed in S26/S27. If the client still requires a paid proof, run exactly one approved Asset Discovery proof and then freeze the intake path.
- Closing document output remains subject to final client/legal approval of the reviewed closing templates.
- Live SMS/email sending remains blocked until outreach compliance and provider decisions are approved.
