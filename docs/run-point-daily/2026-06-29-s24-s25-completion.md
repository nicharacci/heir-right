# HeirRight S24/S25 Completion Evidence - 2026-06-29

Branch: `v1.1.0/heirright-contract-completion-s24-s25`

## Scope Verified

- S22/S23 briefs were read against the attached contract-completion planning source before implementation.
- S24 outreach now persists campaigns/templates through the app storage seam, keeps Draft/Ready/Approved/Sync to Podio/Archived states, records audit events, blocks approval for unauthorized approvers and unresolved variables, and blocks Podio sync without live readiness.
- S24 Google/Podio worker exports now require explicit approval, Podio CSV backup confirmation, controlled readback, and fail-closed credential blockers.
- S24 production artifact app now exposes fail-closed `/api/connections/status` and `/api/exports` responses on the app alias.
- S25 public site has direct `terms.html` and `privacy.html` routes, updated sitemap entries, direct footer/nav links, and hardened form error handling.

## Production Proof

- Public site deployed from `site-v2/` to Vercel project `heirright`: `https://heirright.vercel.app`.
- Operator app deployed from `probate-lead-engine/apps/artifact/` to Vercel project `heirright-landing-demo`: `https://heirright-landing-demo.vercel.app`.
- Desktop and mobile browser proof on `https://heirright.vercel.app/`, `/terms.html`, `/privacy.html`, and `/contact.html`: no horizontal overflow and no console errors.
- Production landing form returned `HR-20260629-B9AF1D22` for controlled test `S25 CONTROLLED TEST - DO NOT WORK`.
- Production contact form returned `HR-20260629-6AA1A0B2` for controlled test `S25 CONTROLLED TEST - DO NOT WORK`.
- Production app alias Settings showed Podio, Google, Resend, and SMS Gateway as blocked with operator next steps.
- Production app `/api/exports` returned HTTP 503 blocked and confirmed no Podio card, Google Doc, Sheet row, email, SMS, or Resend message was created.

## Custom Domain Caveat

- `https://heirright.com/` is currently a WordPress/Cloudflare site, not a Vercel domain under the `solvys` team.
- `https://heirright.com/terms.html` and `/privacy.html` returned 404.
- No DNS or custom-domain change was made because S25 excludes custom domain/DNS changes unless explicitly requested.

## Validation

- `cd probate-lead-engine && pnpm build` passed.
- `pnpm --filter @ple/worker test` passed with 54 facts.
- `pnpm --filter @ple/worker export:dry` passed.
- `pnpm --filter @ple/worker export:podio-live-test` failed closed before write, naming missing Podio config.
- `pnpm --filter @ple/worker export:google-live-test` failed closed before write, naming missing Google Workspace config.
- `pnpm --filter @ple/artifact build` passed.
- `cd site-v2 && pnpm build` passed.
- `git diff --check` passed.

## Notes

- No IDI, enrichment, paid lookup, or live external SMS/email send path was added to S24 or S25.
- Pre-existing dirty dependency edits in `probate-lead-engine/package.json` and `probate-lead-engine/pnpm-lock.yaml` were preserved and left unrelated to this sprint work.
