{
  "name": "heirright-site-v2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:locales": "node scripts/generate-locales.mjs",
    "dev": "pnpm generate:locales && vite --host 127.0.0.1",
    "build": "pnpm generate:locales && tsc && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "lenis": "^1.3.15",
    "three": "^0.181.2"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.4"
  }
}
